package org.example

import org.junit.jupiter.api.Test

class SelenideWork {
    @Test
    fun `should find and go to testflo marketplace page through google, and display latest testflo version`() {
        // println(latestVersion)
    }

    @Test
    fun `should find and go to testflo marketplace page through google, and display all released testflo versions with release date and description`() {
        // format: version - date - description
        // println(allVersions)
    }
}